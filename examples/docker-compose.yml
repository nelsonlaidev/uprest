services:
  redis:
    image: redis:8.10.2
    ports:
      - '6379:6379'

  uprest:
    build:
      context: ..
    ports:
      - '8079:8080'
    environment:
      UPREST_TOKEN: example-token
      UPREST_CONNECTION_STRING: redis://redis:6379
