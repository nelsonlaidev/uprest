[changelog]
header = """
# Changelog

All notable changes to this project will be documented in this file.\n
"""
body = """
{%- macro remote_url() -%}
https://github.com/nelsonlaidev/uprest
{%- endmacro -%}

{%- macro print_commit(commit) -%}
- {% if commit.scope %}_({{ commit.scope }})_ {% endif %}{% if commit.breaking %}[**breaking**] {% endif %}{{ commit.message | upper_first }} - ([{{ commit.id | truncate(length=7, end="") }}]({{ self::remote_url() }}/commit/{{ commit.id }}))
{%- endmacro -%}

{%- if version %}\
    ## {{ version }}\n
    {% if previous.version %}\
    [Compare {{ previous.version }}...{{ version }}]({{ self::remote_url() }}/compare/{{ previous.version }}...{{ version }}) - {{ timestamp | date(format="%Y-%m-%d") }}\n
    {% else %}\
    [View {{ version }}]({{ self::remote_url() }}/tree/{{ version }}) - {{ timestamp | date(format="%Y-%m-%d") }}\n
    {% endif %}\
{% else %}\
    ## [Unreleased]\n
{% endif -%}

{%- if message %}
{{ message }}\n
{% endif -%}

{%- for group, commits in commits | group_by(attribute="group") %}\
    ### {{ group | striptags | trim | upper_first }}\n
    {% for commit in commits %}\
        {{ self::print_commit(commit=commit) }}{% if loop.last %}\n{% endif %}
    {% endfor %}\
{% endfor %}
"""
footer = ""
trim = true

[git]
conventional_commits = true
split_commits = false
sort_commits = "oldest"
filter_commits = false
protect_breaking_commits = false
filter_unconventional = true
topo_order = false
tag_pattern = "v[0-9].*"
commit_parsers = [
    { message = ".*changelog.*", skip = true },
    { message = "^feat", group = "<!-- 0 -->Features" },
    { message = "^fix", group = "<!-- 1 -->Bug Fixes" },
    { message = "^docs", group = "Documentation" },
    { message = "^style", group = "Styling" },
    { message = "^refactor", group = "Refactor" },
    { message = "^perf", group = "Performance" },
    { message = "^test", group = "Testing" },
    { message = "^build", group = "Build System" },
    { message = "^ci", group = "Continuous Integration" },
    { message = "^chore", group = "Chores" },
    { message = "^revert", group = "Reverts" },
]
link_parsers = [
    { pattern = "#(\\d+)", href = "https://github.com/nelsonlaidev/uprest/issues/$1" },
    { pattern = "RFC(\\d+)", href = "https://datatracker.ietf.org/doc/html/rfc$1" },
]
